//! In-memory implementation of `ego-persistence-api`'s `operation`
//! reservation port — `OperationReservationStore` — relocated verbatim from
//! `ego-testkit` (design.md AD-3, AD-5).

pub mod reservation;
