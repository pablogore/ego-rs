[package]
name = "ego-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
ego-domain = { path = "../domain" }
chrono = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
mockall = "0.12"
