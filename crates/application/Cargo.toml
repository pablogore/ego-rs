[package]
name = "ego-application"
version = "0.1.0"
edition = "2021"

[dependencies]
ego-domain = { path = "../domain" }
async-trait = "0.1"

[dev-dependencies]
mockall = { workspace = true }
tokio-test = "0.4"
