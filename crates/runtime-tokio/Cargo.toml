[package]
name = "ego-runtime-tokio"
version = "0.1.0"
edition = "2021"

[dependencies]
ego-domain = { path = "../domain" }
ego-runtime = { path = "../runtime" }
tokio = { version = "1", features = ["full"] }
