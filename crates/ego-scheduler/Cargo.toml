[package]
name = "ego-scheduler"
version = "0.1.0"
edition = "2021"

[dependencies]
ego-domain = { path = "../domain" }
tokio = { version = "1.0", features = ["sync"] }
tracing = "0.1"
thiserror = "1.0"
sha2 = "0.11.0"
