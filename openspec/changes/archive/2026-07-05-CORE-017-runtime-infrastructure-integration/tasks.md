# Tasks: CORE-017 — Runtime Infrastructure Integration

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~330-400 |
| 400-line budget risk | Medium |
| Chained PRs recommended | No |
| Suggested split | Single PR (all changes confined to `crates/service-sdk`) |
| Delivery strategy | ask-on-risk (default, not yet decided) |
| Chain strategy | pending |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: pending
400-line budget risk: Medium

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | Single PR | PR 1 | All phases below ship together; single crate (`ego-service-sdk`), no cross-crate fan-out. Re-forecast at apply time if actual diff exceeds ~400 lines — split Phase 5's integration tests into a follow-up PR if so. |

### Post-Apply Re-Forecast (`size:exception`)

Actual diff: ~800 lines of code/tests (`crates/service-sdk/`) plus ~1135
lines of SDD process docs (proposal/design/tasks/apply-progress/verify-report)
— roughly double the original ~330-400 estimate. Flagged during the pre-PR
review fan-out (R2 Readability). Decision: ship as a single PR with an
explicit `size:exception` rather than splitting. A documentation/code split
was evaluated and rejected because the additional review overhead outweighed
the benefit once the implementation had already completed multiple
independent review rounds (four `/code-review` passes plus the R1-R4 pre-PR
fan-out).

## Phase 1: Foundation — Dependencies + ConfigurationProvider

- [x] TASK-001 In `crates/service-sdk/Cargo.toml`: move `serde` (`features = ["derive"]`) from `[dev-dependencies]` to `[dependencies]`; add `serde_json = "1"` and `kitlogger-formatter` (git, same repo/branch as the existing `kitlogger` dep) as real dependencies. Run `cargo build -p ego-service-sdk` to confirm the new git dependency resolves. (Design: File Changes, Open Question "New git dependencies resolve") — DONE. Also added `console-exporter` and `kitlogger-log-domain` as `[dev-dependencies]` (same git repo/branch): not called out in design.md's File Changes table, but required to construct `Arc<ConsoleExporterImpl>` and `Severity` for the capture-buffer tests TASK-008/TASK-022 already call for — kitlogger's crate root re-exports neither type (design.md's own grounding finding). `cargo build -p ego-service-sdk` confirms all four git deps resolve.
- [x] TASK-002 RED: create `crates/service-sdk/src/runtime/config_provider.rs` with a test module asserting `ConfigurationProvider::from_value(json!({"logging": {"enabled": true, "format": "json"}})).logging()` returns `Ok(LoggingSettings { enabled: true, format: LogFormatSetting::Json })`, and a malformed `format` value returns `Err(RuntimeInfraError::ConfigInvalid { .. })`. (Design: Interfaces/Contracts — ConfigurationProvider) — DONE. Also added a malformed-type case (`enabled: "yes"`) and a missing-subtree case; the latter revealed a real behavior worth noting (see TASK-004).
- [x] TASK-003 GREEN: implement `ConfigurationProvider { root: serde_json::Value }`, `from_value`, `logging()`, `LoggingSettings` (`#[serde(default)]`), `LogFormatSetting` (`#[serde(rename_all = "lowercase")]`), and `RuntimeInfraError` (thiserror; exactly `ConfigInvalid`, `LoggerInit`, `Teardown`) in `config_provider.rs`, per design.md's Interfaces/Contracts block verbatim, to pass TASK-002. DONE — all `cargo test -p ego-service-sdk` config_provider tests pass.
- [x] TASK-004 REFACTOR: add module docs to `config_provider.rs` stating `ConfigurationProvider` is a thin host-boundary role (no sources/merge/parse — kit-config owns those). Run `cargo test -p ego-service-sdk config_provider`. DONE. **Discovery**: `#[serde(default)]` on `LoggingSettings` fills missing *fields* within a map; it does not rescue deserialization when the top-level `logging` node is entirely absent (`.get("logging").cloned().unwrap_or_default()` yields `Value::Null`, and `serde_json::from_value::<LoggingSettings>(Value::Null)` fails — a struct can't deserialize from `null`). So an absent `logging` key is `ConfigInvalid`, not "all defaults" — hosts must supply at least `"logging": {}` to get field-level defaults. This is the literal, frozen `Interfaces/Contracts` code as written in design.md; not something this task changed. Documented via `logging_missing_subtree_is_config_invalid` / `logging_empty_object_uses_defaults` tests.

## Phase 2: Logger Boundary Adapter

- [x] TASK-005 RED: create `crates/service-sdk/src/runtime/logger.rs` with a test module asserting `build_logger` maps each `LogFormatSetting` variant (`Json`, `Pretty`, `Compact`, `Text`) to the expected `kitlogger_formatter::LogFormat` (table test), and that `build_logger(&LoggingSettings { enabled: false, .. })` returns `Ok(None)` with no `KITLogger` constructed. (Design: Testing Strategy rows 2-3)
- [x] TASK-006 GREEN: implement `pub fn build_logger(cfg: &LoggingSettings) -> Result<Option<Arc<KITLogger>>, RuntimeInfraError>` in `logger.rs` exactly per design.md's Interfaces/Contracts (format mapping, `KITLogger::with_format(fmt)`, `logger.init()` mapped to `LoggerInit { reason: format!("{e:?}") }`) to pass TASK-005. `AdapterError: Debug` is confirmed (`#[derive(Debug, ...)]` in `telemetry-adapter-contracts::error::AdapterError`, verified against the kitlogger source) — the `{e:?}` mapping compiles as specified, no further verification needed.
- [x] TASK-007 REFACTOR: add the doc comment marking `build_logger` as THE canonical **public** logger construction entry point, explicitly warning against adding another *public* helper that constructs `KITLogger` a different way (a private internal helper used only by `build_logger`'s own implementation is fine — the restriction is on the public surface). Run `cargo test -p ego-service-sdk logger`. Document the `init()`-failure test (Design: Testing Strategy row 4) as untestable at this layer if kitlogger exposes no failing-exporter test seam — do not fabricate one. DONE — with a refinement: `build_logger` itself has no test seam (it always constructs a *fresh* `KITLogger` internally, whose exporter starts `Uninitialized`, so its `init()` call can never fail through `build_logger`'s own public signature). However kitlogger *does* expose a real, non-fabricated failure surface: initializing the same shared `ConsoleExporterImpl` exporter twice via `KITLogger::with_exporter_and_format` is an invalid lifecycle transition (`Running -> Running`) that produces a real `AdapterError`. `logger_init_failure_maps_to_logger_init_error` drives that real failure through the identical `map_err` expression `build_logger` uses, verifying the mapping (not fabricating a failure path inside `build_logger` that cannot occur).
- [x] TASK-008 RED: in `logger.rs`, add a private `TeardownStack` test module asserting `drain()` shuts down entries in reverse (LIFO) construction order using a capture-buffer logger (`KITLogger::with_exporter_and_format`), and that a second `drain()` call on an already-drained stack returns `Ok(())`. (Design: Testing Strategy rows 6-7) — DONE with a documented deviation: kitlogger's real `shutdown()`/`flush()` provide no externally observable side channel to time-order two independent shutdowns from the outside (`ConsoleExporterImpl::flush()` is a stub that always returns `Ok(())` without touching writers), so "verify order with a capture logger" as design.md's Testing Strategy row 6 suggested is not black-box observable with the current kitlogger API. The test instead verifies the actual guarantee: `push` preserves insertion order in `entries` (asserted directly, same module) and `Vec::pop` is LIFO by language definition, then confirms `drain()` performs real teardown (not a no-op) by asserting both loggers are unusable afterward.
- [x] TASK-009 GREEN: implement private `TeardownStack { entries: Vec<Arc<KITLogger>> }` with `new()`, `push()`, `drain()` (LIFO pop; collects the first error as `RuntimeInfraError::Teardown`) in `logger.rs`, exactly per design.md's Interfaces/Contracts, to pass TASK-008. DONE.

## Phase 3: RuntimeBuilder + RuntimeInner Integration

- [x] TASK-010 RED: in `crates/service-sdk/src/runtime/builder.rs` tests, assert `RuntimeBuilder::new().build().inner().logger()` is `None`, and `RuntimeBuilder::new().with_logger(Arc::new(KITLogger::default())).build().inner().logger()` is `Some`. (Design: Testing Strategy row 5)
- [x] TASK-011 GREEN: add `logger: Option<Arc<KITLogger>>` to `RuntimeBuilder` and `pub fn with_logger(mut self, logger: Arc<KITLogger>) -> Self`, mirroring `.with_security(..)` exactly. Builder-only change — `build()` is not touched yet, so this compiles but TASK-010's assertion on `.build()` still fails until TASK-013.
- [x] TASK-012 GREEN: in `crates/service-sdk/src/runtime/runtime_builder.rs`, add `logger: Option<Arc<KITLogger>>` and `teardown: Mutex<TeardownStack>` fields to `RuntimeInner`; add `new_with_logger(..)` and a `#[doc(hidden)]` `pub fn logger(&self) -> Option<&Arc<KITLogger>>` accessor (macro-facing, per the existing AD-7 pattern on `authorization_provider()`); update `Default for RuntimeInner` to set `logger: None` and an empty `Mutex<TeardownStack>`. Resolve the "deferred to TASK-013/TASK-014" module-doc note for config+logger construction only — leave the tenant-enforcement/graph-validation deferral untouched. DONE.
- [x] TASK-013 GREEN: wire `RuntimeBuilder::build()` to push the logger (if any) onto a `TeardownStack` and construct `RuntimeInner` via `new_with_logger(..)`. `build()` stays `pub fn build(self) -> Runtime` (infallible). This makes TASK-010 pass.
- [x] TASK-014 RED: in `builder.rs` tests, assert `Runtime::shutdown()` on a runtime built with `.with_logger(..)` returns `Ok(())`, and calling it a second time also returns `Ok(())`. Uses an `initialized_logger()` test helper (calls `KITLogger::init()` before wrapping in `Arc`) since real host bootstrap always initializes before `.with_logger(..)` — an un-initialized logger's `shutdown()` fails for an unrelated reason (invalid `Uninitialized -> Flushing` transition).
- [x] TASK-015 GREEN: implement `pub fn shutdown(&self) -> Result<(), RuntimeInfraError>` on `Runtime`: `self.inner.teardown.lock().expect("teardown mutex poisoned").drain()`.
- [x] TASK-016 Ownership test — DONE with a documented deviation from the literal wording. tasks.md says to assert `Arc::strong_count(&logger) == 1` after `rt.shutdown()` ("back to only the test's reference"). Tracing design.md's own frozen `build()`/`new_with_logger` code shows that's unreachable as specified: `RuntimeInner` retains its own **permanent** `logger: Option<Arc<KITLogger>>` field (so `.logger()` keeps working after shutdown) *in addition to* the separate clone `TeardownStack` holds for ordered teardown — two independent long-lived owners by design (File Changes table: "RuntimeInner gains `logger: Option<Arc<KITLogger>>` + `Mutex<TeardownStack>`"). `shutdown()` only drains the stack's clone; the count after shutdown is 2 (test's + `RuntimeInner.logger`), not 1. The test instead asserts the part of the contract that does hold unconditionally: `Arc::strong_count(&logger) > 1` after `.build()`, and `Arc::strong_count(&logger)` strictly *decreases* after `.shutdown()` — this still catches a real leak (the property TASK-016 exists for) without asserting an unreachable exact value. Saved to engram as a design/reality mismatch.
- [x] TASK-017 REFACTOR: run `cargo test -p ego-service-sdk runtime`; confirm no existing `RuntimeBuilder::new().build()` call site needed a change (Design: Migration/Rollout). DONE — all 53 lib tests pass; `examples/reference-app` and other `RuntimeBuilder::new().build()`/`with_security(..).build()` call sites needed no changes.

## Phase 4: ServiceContext Access

- [x] TASK-018 RED: in `crates/service-sdk/src/context/mod.rs` tests, assert `ServiceContext::new().logger()` is `None`, and `ServiceContext::new().with_logger(Arc::new(KITLogger::default())).logger()` is `Some`. DONE — `logger_is_none_by_default` / `with_logger_sets_logger`.
- [x] TASK-019 GREEN: add `logger: Option<Arc<KITLogger>>` to `ServiceContext`; add `with_logger(mut self, logger: Arc<KITLogger>) -> Self` and `logger(&self) -> Option<&KITLogger>`, mirroring the existing `security`/`with_security`/`security()` triplet exactly; default `logger: None` in `ServiceContext::new()`. DONE. **Side effect discovered**: `KITLogger` does not implement `Debug`, so `#[derive(Debug, Clone)]` on `ServiceContext` no longer compiles with the new field. Replaced the derive with a hand-rolled `Debug` impl (mirrors `RuntimeInner`'s own hand-rolled `Debug` in `runtime_builder.rs`, which hits the same constraint for the same field type) that prints `logger` as `self.logger.is_some()` rather than the value itself.
- [x] TASK-020 Add `mod config_provider;` and `mod logger;` to `crates/service-sdk/src/runtime/mod.rs`; re-export `ConfigurationProvider`, `LoggingSettings`, `LogFormatSetting`, `RuntimeInfraError` (from `config_provider`) and `build_logger` (from `logger`). Already done by the prior (interrupted) apply run alongside Phase 1-3 work — verified present, no duplicate `mod`/re-export added.

## Phase 5: Public Surface + Integration Verification

- [x] TASK-021 Integration test — bootstrap path only: `ConfigurationProvider::from_value(..)` → `.logging()` → `build_logger(..)` → `RuntimeBuilder::new().with_logger(logger).build()` → `ServiceContext::new().with_logger(rt.inner().logger().unwrap().clone())`; assert `ctx.logger()` is `Some` and points at the same logger. Manual `ServiceContext::new().with_logger(..)` construction is intentional here, not a gap: there is no generated-dispatcher path that assembles `ServiceContext` today — the example app constructs `.with_security(..)` the same manual way, so this test follows the codebase's current real pattern rather than inventing a shortcut. (Design: Testing Strategy row 8, bootstrap half) DONE — `runtime::integration_tests::bootstrap_path_wires_logger_from_config_to_service_context` in `crates/service-sdk/src/runtime/mod.rs`. Per the testing skill's Decision Gates (only in-memory state, no real DB/broker/HTTP), this lives as an ordinary `#[cfg(test)]` module, not in `crates/integration-tests/`. Updated post-review: the test now calls the public `Runtime::logger()` facade (added after this task shipped) instead of the hidden `RuntimeInner::logger()` shown in this task's original description above.
- [x] TASK-022 Integration test — shutdown path only, isolated from TASK-021 so a failure points at one component instead of six: build a `Runtime` with `.with_logger(..)` wired to a capture-buffer exporter (`KITLogger::with_exporter_and_format`), call `rt.shutdown()`, assert the buffer received a flush with no lost records. (Design: Testing Strategy row 8, shutdown half) DONE — `runtime::integration_tests::shutdown_path_flushes_capture_buffer_with_no_lost_records`. **Grounding note** (same real-API constraint already documented for TASK-008): `ConsoleExporterImpl::export()` writes to the router synchronously on every `log()` call; `shutdown()`'s flush is a documented no-op stub. So "flush with no lost records" is verified as: log N records, call `rt.shutdown()`, assert all N are present in the capture buffer and `shutdown()` returns `Ok(())` — this catches both data loss and a broken teardown path, without asserting a flush side-channel kitlogger's stub doesn't provide.
- [x] TASK-023 Run `cargo test --workspace` and `cargo build --workspace`; confirm `kit-config` and `kitlogger` remain unmodified and compile standalone (Proposal: Success Criteria); confirm no crate other than `ego-service-sdk` gained a new dependency. DONE — see Results below. There is no `kit-config` crate in this workspace (confirmed via `fd`); nothing to check there beyond its absence. `rg -l "kitlogger" --glob "**/Cargo.toml"` returns only `crates/service-sdk/Cargo.toml` — no other crate gained a kitlogger-family git dependency.
- [x] TASK-024 Architectural conformance check (grep, not a build step): confirm no direct `KITLogger` construction exists in any crate outside `runtime/logger.rs` (Proposal: Success Criteria — "services never construct loggers directly"). DONE. Grepped `KITLogger::(new|with_format|with_config|with_exporter_and_format|default)(` across `examples/` and `crates/` outside `service-sdk`: zero matches — no application/service code constructs a logger directly. Within `service-sdk` itself, direct construction exists only inside `#[cfg(test)]` fixtures (`context/mod.rs`, `runtime/builder.rs`, `runtime/mod.rs`'s `integration_tests`) — consistent with the already-accepted TASK-010/014/016 test pattern — and inside `build_logger` in `runtime/logger.rs` itself, the canonical construction point. No production code outside `logger.rs` constructs a `KITLogger`.

## Acceptance Criteria (per task)

Each TASK above is verifiable by: the RED sub-step fails before its GREEN counterpart, `cargo test -p ego-service-sdk` passes after GREEN, and neither `kit-config` nor `kitlogger` source is modified (only `service-sdk` files change).
